/**
 * Created by usamaahmed on 4/7/17.
 */
import { getAuthToken } from './helpers/auth';

export const initialState = {
    properties: {
        isLoading: true,
        message: '',
        element: {}
    }
}